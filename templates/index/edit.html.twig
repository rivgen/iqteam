{% extends 'base.html.twig' %}
{% block body %}
    <div class="slash intoPage">
        <h1>Блок( {{ id }} ) главной страницы</h1>

        {{ include('index/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('index_index') }}">back to list</a>

    </div>
{% endblock %}